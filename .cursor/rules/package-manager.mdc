---
description: 패키지 매니저로 항상 pnpm을 사용
alwaysApply: true
---

# 패키지 매니저 규칙

- 의존성 설치 시 **항상 pnpm**을 사용한다.
- `npm install`, `npm add`, `yarn add` 대신 `pnpm install`, `pnpm add`를 사용한다.
- 스크립트 실행도 `pnpm run dev`, `pnpm run build` 등 pnpm으로 수행한다.
