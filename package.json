{
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "workspaces": [
    "app",
    "functions"
  ],
  "scripts": {
    "proxy": "node scripts/setup-proxy.js",
    "serve": "firebase emulators:start --only functions",
    "dev": "pnpm --filter app dev",
    "build": "pnpm -r build",
    "push": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^13.30.1"
  }
}
