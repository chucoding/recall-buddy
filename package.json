{
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "workspaces": [
    "app",
    "functions"
  ],
  "scripts": {
    "proxy": "node scripts/setup-proxy.js",
    "serve": "pnpm --filter functions build && firebase emulators:start --only functions",
    "dev": "pnpm --filter app dev",
    "build": "pnpm -r build",
    "publish": "firebase deploy"
  }
}
