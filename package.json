{
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "workspaces": [
    "app",
    "functions",
    "shared"
  ],
  "scripts": {
    "proxy": "node scripts/setup-proxy.js",
    "dev": "concurrently --kill-others \"firebase emulators:start\" \"pnpm --filter app run dev\"",
    "build": "pnpm -r run build",
    "push": "firebase deploy --only hosting,functions"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
