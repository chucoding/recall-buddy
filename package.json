{
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "workspaces": [
    "app",
    "functions"
  ],
  "scripts": {
    "dev": "pnpm -C app dev",
    "build": "pnpm -C app build && pnpm -C functions build",
    "deploy": "firebase deploy --only hosting,functions"
  }
}
